# Benchmarks

These pages provide repeatable fixtures for comparing framework output across common UI surfaces.

- `dashboard.html`: operational UI with stats and tables.
- `form.html`: dense form page.
- `marketing.html`: responsive landing content.

Run:

```sh
npm run benchmark
```

The script writes `benchmarks/results.json` with current bundle sizes and fixture metadata.
